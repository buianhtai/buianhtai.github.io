{
  "schema_version": 2,
  "diagram_type": "workflow",
  "meta": {
    "title": "Async Execution: Eager Future vs Poll-Driven Future",
    "quality_profile": "showcase"
  },
  "lanes": [
    { "id": "java", "label": "Java: CompletableFuture", "variant": "exception" },
    { "id": "rust", "label": "Rust: async fn + Tokio" }
  ],
  "phases": [
    { "id": "create", "label": "Create", "fromCol": 0, "toCol": 1 },
    { "id": "run", "label": "Run", "fromCol": 2, "toCol": 3 }
  ],
  "mainPath": ["j_create", "j_run", "j_done"],
  "nodes": [
    { "id": "j_create", "lane": "java", "col": 0, "type": "backend", "label": "supplyAsync(...)", "sublabel": "starts immediately", "width": 160 },
    { "id": "j_run", "lane": "java", "col": 2, "type": "external", "label": "Runs on Executor", "sublabel": "eager — work begins now", "width": 160 },
    { "id": "j_done", "lane": "java", "col": 3, "type": "backend", "label": "Completes", "sublabel": "callback fires", "width": 160 },
    { "id": "r_create", "lane": "rust", "col": 0, "type": "backend", "label": "async fn call()", "sublabel": "returns a Future — does nothing yet", "width": 160 },
    { "id": "r_run", "lane": "rust", "col": 2, "type": "external", "label": "Tokio polls it", "sublabel": "runtime decides when to run", "width": 160 },
    { "id": "r_done", "lane": "rust", "col": 3, "type": "backend", "label": "Resolves", "sublabel": ".await unwraps the value", "width": 160 }
  ],
  "edges": [
    { "id": "j1", "from": "j_create", "to": "j_run", "variant": "default" },
    { "id": "j2", "from": "j_run", "to": "j_done", "variant": "default" },
    { "id": "r1", "from": "r_create", "to": "r_run", "variant": "emphasis" },
    { "id": "r2", "from": "r_run", "to": "r_done", "variant": "emphasis" }
  ],
  "cards": [
    { "dot": "rose", "title": "Java: eager", "items": ["supplyAsync submits work to an executor immediately on creation", "The JVM/executor is always present — no runtime to choose"] },
    { "dot": "cyan", "title": "Rust: nothing runs without a runtime", "items": ["An async fn call produces an inert Future — creation does no work", "The standard library ships no runtime; Tokio is the de facto choice you add yourself"] }
  ]
}
