---
title: "The Agent Flywheel: 12 Tools That Make Multi-Agent Coding Actually Work"
description: "A full map of Jeffrey Emanuel's 12-tool agentic coding ecosystem, organized as a flywheel loop: Plan → Coordinate → Execute → Verify → Remember."
pubDate: 2026-03-22
tags: ["ai", "agents", "architecture", "rust", "golang"]
lang: "en"
category: "architecture"
series: "ai-agent-tools"
seriesOrder: 4
---

import Card from '../../../components/mdx/Card.astro';
import Label from '../../../components/mdx/Label.astro';
import FlowStep from '../../../components/mdx/FlowStep.astro';
import HeroGrid from '../../../components/mdx/HeroGrid.astro';
import Callout from '../../../components/mdx/Callout.astro';
import ArchLayer from '../../../components/mdx/ArchLayer.astro';
import Pipeline from '../../../components/mdx/Pipeline.astro';
import DataTable from '../../../components/mdx/DataTable.astro';

Running multiple AI coding agents isn't hard. Making them work *together* without race conditions, duplicate effort, unsafe shell commands, and lost institutional memory is the hard part. Jeffrey Emanuel's ecosystem now spans **12 focused tools** that collectively solve that systems problem.

The key insight is architectural, not cosmetic: these tools are designed as a loop. You **plan work**, **coordinate agents**, **execute safely**, **verify aggressively**, and **remember outcomes** so the next cycle starts with better priors.

<HeroGrid items={[
  { label: "12 tools", sublabel: "End-to-end loop\nPlan → Remember", color: "#0891b2" },
  { label: "7,300+ stars", sublabel: "11 public repos\nplus CAAM auth layer", color: "#059669" },
  { label: "5 stages", sublabel: "Plan, Coordinate\nExecute, Verify, Remember", color: "#d97706" },
  { label: "multi-runtime", sublabel: "Rust, Go, Bash\nPython, TypeScript", color: "#e11d48" },
]} />

---

## WHY this matters in production

<DataTable
  headers={["Failure Mode", "What breaks", "Flywheel countermeasure"]}
  rows={[
    {
      "Failure Mode": "Task chaos",
      "What breaks": "Agents pick low-value work or block each other",
      "Flywheel countermeasure": "beads_rust + beads_viewer dependency-aware prioritization"
    },
    {
      "Failure Mode": "Coordination collisions",
      "What breaks": "Two agents edit same files, duplicate or conflicting output",
      "Flywheel countermeasure": "Agent Mail reservations + Agent Farm lock-based orchestration"
    },
    {
      "Failure Mode": "Unsafe execution",
      "What breaks": "Destructive shell/database/cloud commands slip through",
      "Flywheel countermeasure": "DCG multi-tier command interception"
    },
    {
      "Failure Mode": "Silent code regressions",
      "What breaks": "LLM-generated bugs survive to commit",
      "Flywheel countermeasure": "UBS language-specific pattern scanning"
    },
    {
      "Failure Mode": "Knowledge amnesia",
      "What breaks": "Teams repeatedly solve already-solved problems",
      "Flywheel countermeasure": "CASS session retrieval + CM rule distillation"
    }
  ]}
  caption="The flywheel is useful because it closes concrete operational failure loops."
  accent="teal"
/>

<Callout color="amber">
The ecosystem is strongest when used as a loop, not as a menu of independent tools.
</Callout>

## Full 12-tool inventory mapped to the flywheel

<DataTable
  headers={["Stage", "Tools", "Role in the loop"]}
  rows={[
    {
      Stage: "Plan",
      Tools: "beads_rust, beads_viewer",
      "Role in the loop": "Track work in a dependency DAG and prioritize by graph metrics (PageRank, critical path, bottlenecks)."
    },
    {
      Stage: "Coordinate",
      Tools: "Agent Mail, Agent Farm, NTM, SLB",
      "Role in the loop": "Message agents, reserve files, orchestrate lock-safe parallel workers, and launch multi-process sessions quickly."
    },
    {
      Stage: "Execute",
      Tools: "ACFS, CAAM",
      "Role in the loop": "Bootstrap environments and keep OAuth/account context valid so agents can actually run end-to-end workflows."
    },
    {
      Stage: "Verify",
      Tools: "UBS, DCG",
      "Role in the loop": "Catch bug patterns and block catastrophic commands before damage reaches git or infrastructure."
    },
    {
      Stage: "Remember",
      Tools: "CASS, CM",
      "Role in the loop": "Index sessions and promote learnings into durable procedural rules for later reuse."
    }
  ]}
  caption="Plan → Coordinate → Execute → Verify → Remember"
  accent="green"
/>

## <Label color="teal">1</Label> beads_rust — Issue Tracking in Rust

[beads_rust](https://github.com/Dicklesworthstone/beads_rust) (`br`) is a Rust port of Steve Yegge's beads tracker, frozen at the "classic" SQLite + JSONL architecture. 762 stars.

<Card>
### Why a Rust Rewrite?

When Yegge evolved beads toward GasTown with new approaches, Emanuel's entire agent system depended on the classic architecture. Rather than fight merge conflicts on a Go fork, he rewrote in Rust — with Yegge's endorsement.

**Key differentiator:** Pure-Rust SQLite via FrankenSQLite (no C bindings), single-binary distribution, and built-in MCP server support.

```bash
br ready              # Shows unblocked issues (same as bd)
br create "Fix auth" --priority 1
br dep add bd-abc bd-xyz
br sync               # Import/export JSONL
```
</Card>

<Card>
### Atomic JSONL Writes

```rust
struct TempFileGuard {
    path: PathBuf,
    persist: bool,
}

impl Drop for TempFileGuard {
    fn drop(&mut self) {
        if !self.persist {
            let _ = fs::remove_file(&self.path);
        }
    }
}
```

Write to temp file → rename on success. If the process crashes mid-write, the guard auto-cleans. No corrupted JSONL files.
</Card>

---

## <Label color="green">2</Label> beads_viewer — Graph-Aware Triage

[beads_viewer](https://github.com/Dicklesworthstone/beads_viewer) (`bv`) transforms flat issue lists into a dependency graph and extracts actionable intelligence. 1,408 stars. Go + ForceGraph + D3 + WASM.

<Card>
### Nine Graph Metrics

Not just "what's open" — `bv` computes which issues *matter most*:

| Metric | What It Measures |
|--------|-----------------|
| **PageRank** | Recursive dependency importance |
| **Betweenness** | Bridge nodes connecting clusters |
| **HITS** | Hub/authority duality (epics vs utilities) |
| **Critical Path** | Longest dependency chain (zero-slack) |
| **Eigenvector** | Influence via connected neighbors |
| **K-Core** | Densely connected subgraphs |
| **Articulation** | Nodes whose removal disconnects the graph |
| **Cycles** | Circular dependencies (errors!) |
| **Slack** | Parallelizable tasks with float |

</Card>

<Card>
### Robot Mode for Agents

Never run bare `bv` in an agent context — always use `--robot-*`:

```bash
bv --robot-triage    # Full triage: top picks + claim commands
bv --robot-next      # Single highest-priority unblocked task
bv --robot-insights  # All 9 metrics as JSON
bv --robot-plan      # Parallel execution tracks
```

Output includes ready-to-run claim commands: `bd claim bd-123` — zero friction from analysis to action.
</Card>

<Card>
### What-If Simulation

Click any node to simulate closing it. The WASM engine cascades through the DAG, showing which blocked issues would unblock:

```javascript
export function performWhatIf(node) {
    const closedSet = buildClosedSet();
    let result = store.wasmGraph.whatIfClose(idx, closedSet);
    animateWhatIfCascade(node, result);
    return result;
}
```

The animation ripples outward from the closed node — green highlights for newly unblocked tasks. "If I close this, what opens up?"
</Card>

---

## <Label color="amber">3</Label> cass — Session Search Across 11 Agents

[coding_agent_session_search](https://github.com/Dicklesworthstone/coding_agent_session_search) (`cass`) aggregates session histories from Claude Code, Codex, Cursor, ChatGPT, Cline, Aider, OpenCode, Gemini CLI, Pi-Agent, Amp, and Factory into a single searchable index. Rust, MIT license.

<Card>
### Three-Tier Search

<FlowStep step={1} color="amber" title="Lexical (BM25 + N-grams)">
FrankenSQLite FTS5 + Tantivy for full-text search. Edge n-grams for prefix matching. Fast, cheap, handles typos.
</FlowStep>

<FlowStep step={2} color="amber" title="Semantic (Embeddings)">
Two strategies: FNV-1a hash embeddings (deterministic, no GPU) or FastEmbed MiniLM model (ML-quality). Progressive refinement: fast hash narrows candidates, ML refines.
</FlowStep>

<FlowStep step={3} color="amber" title="Hybrid (RRF Fusion)">
Reciprocal Rank Fusion merges BM25 + semantic rankings. K=60. Results appearing in both rankers get boosted.
</FlowStep>

</Card>

---

## <Label color="rose">4</Label> cass-memory — Cognitive Memory for Agents

[cass_memory_system](https://github.com/Dicklesworthstone/cass_memory_system) (`cm`) transforms raw session logs into confidence-tracked rules that persist across all agents. 290 stars. TypeScript/Bun, MCP server.

<Card>
### Three-Layer Cognitive Architecture

| Layer | Purpose | Example |
|-------|---------|---------|
| **Episodic** | Raw session logs | Via `cass` CLI search |
| **Working** | Structured session summaries | Diary entries with decisions, challenges |
| **Procedural** | Distilled rules ("bullets") | "Always check for null before `.map()`" |

Rules have maturity levels: **candidate → established → proven → deprecated**. Confidence decays exponentially with a 90-day half-life unless reinforced by positive feedback.
</Card>

<Card>
### Scoring with Decay

```typescript
// Exponential decay: rules lose relevance unless reinforced
effectiveScore = (decayedHelpful - 4 * decayedHarmful) * maturityMultiplier

// Maturity multipliers:
// candidate=0.5, established=1.0, proven=1.5
```

A rule that helped 10 times a month ago scores higher than one that helped 20 times a year ago. Harmful feedback is weighted 4x — one "this broke my build" outweighs four "this was useful."

</Card>

---

## <Label color="purple">5</Label> destructive_command_guard — The Safety Net

[destructive_command_guard](https://github.com/Dicklesworthstone/destructive_command_guard) (`dcg`) intercepts catastrophic shell commands before AI agents execute them. Rust, 49+ security packs, sub-millisecond.

<Card>
### Three-Tier Detection

<FlowStep step={1} color="purple" title="Tier 1: Quick Reject (<10μs)">
SIMD-accelerated `memchr` keyword match. No dangerous keywords → ALLOW immediately (fast path). Most commands never reach Tier 2.
</FlowStep>

<FlowStep step={2} color="purple" title="Tier 2: Safe Patterns">
Known-safe patterns matched first to reduce false positives. `rm -rf /tmp/build` is fine — `/tmp/*` is on the safe list.
</FlowStep>

<FlowStep step={3} color="purple" title="Tier 3: Danger Patterns → BLOCK">
Full regex evaluation with suggestions. Every blocked command includes a safer alternative: `git stash` instead of `git reset --hard`.
</FlowStep>

</Card>

<Card>
### 49+ Security Packs

Coverage across 14 categories:

<ArchLayer badge="CORE" badgeColor="rose" label="Always On" items={["git", "filesystem"]} />
<ArchLayer badge="DB" badgeColor="amber" label="Databases" items={["postgresql", "mysql", "mongodb", "redis", "sqlite"]} />
<ArchLayer badge="K8S" badgeColor="teal" label="Infrastructure" items={["kubectl", "helm", "terraform", "ansible", "pulumi"]} />
<ArchLayer badge="CLOUD" badgeColor="green" label="Cloud" items={["aws", "azure", "gcp", "s3", "secrets"]} />

<DataTable
  headers={["Category", "Representative Packs", "Notes"]}
  rows={[
    {
      Category: "Always On",
      "Representative Packs": "git, filesystem",
      Notes: "Loaded by default for every command evaluation"
    },
    {
      Category: "Databases",
      "Representative Packs": "postgresql, mysql, mongodb, redis, sqlite",
      Notes: "Protects destructive DDL/DML and data-loss operations"
    },
    {
      Category: "Infrastructure",
      "Representative Packs": "kubectl, helm, terraform, ansible, pulumi",
      Notes: "Blocks high-blast-radius infra mutations"
    },
    {
      Category: "Cloud",
      "Representative Packs": "aws, azure, gcp, s3, secrets",
      Notes: "Guards account-wide, bucket-wide, and secret operations"
    }
  ]}
  caption="Representative coverage from the 49+ security packs by category."
  accent="rose"
/>

Every pack defines patterns like:
```rust
destructive_pattern!(
    "reset-hard",
    r"(?:^|[^[:alnum:]_-])git\s+(?:\S+\s+)*reset\s+--hard",
    "git reset --hard destroys uncommitted changes.",
    Critical,
    &[PatternSuggestion::new("git stash", "Save changes first")]
)
```
</Card>

---

## <Label color="teal">6</Label> ultimate_bug_scanner — Quality Gate

[ultimate_bug_scanner](https://github.com/Dicklesworthstone/ultimate_bug_scanner) (`ubs`) is a static analysis tool that catches bugs LLMs commonly introduce. Bash + ripgrep, 9 languages, 18 detection categories. 203 stars.

<Card>
### What It Catches

<DataTable
  headers={["Severity", "Categories"]}
  rows={[
    {
      Severity: "Critical",
      Categories: "Null safety, XSS/injection, async/await bugs, memory leaks, type coercion"
    },
    {
      Severity: "Warning",
      Categories: "Division safety, resource lifecycle, error handling, promise chains"
    },
    {
      Severity: "Info",
      Categories: "Debug code, TODO markers, TypeScript strictness"
    }
  ]}
  caption="Core bug classes flagged by ultimate_bug_scanner."
  accent="teal"
/>

**Language-specific:** React hooks deps (JS), `open()` without `with` (Python), nil pointers (Go), `.unwrap()` panics (Rust), buffer overflows (C/C++).
</Card>

<Card>
### Agent Integration

Not an MCP tool — integrates via hooks:

```bash
# Claude Code: scans on every file write
.claude/hooks/on-file-write.sh

# Git: pre-commit quality gate  
.git/hooks/pre-commit

# Token-optimized output (~50% smaller)
ubs . --format=toon
```

10,000+ lines/second. Use `--staged` for changed-files-only scanning in CI.
</Card>

---

## <Label color="green">7</Label> Agent Mail — Coordination + File Reservations

[MCP Agent Mail](https://github.com/Dicklesworthstone/mcp_agent_mail) gives agents a protocol for messaging and mutual exclusion. This is what turns "many agents" into "cooperating agents".

<Card>
### Why it matters

Without reservations, parallel agents race on shared files. Agent Mail provides explicit reservation + release semantics, ACK flows, and thread context so distributed work doesn't collapse into merge conflict cleanup.

Typical primitives:
- initialize a session
- reserve paths (exclusive/non-exclusive)
- send update/blocked/completed messages
- acknowledge + release
</Card>

---

## <Label color="amber">8</Label> Agent Farm — Lock-Based Parallel Orchestration

[Claude Code Agent Farm](https://github.com/Dicklesworthstone/claude_code_agent_farm) operationalizes parallel workers with lock-based conflict prevention.

<Card>
### Core pattern

Agent Farm doesn't depend on fragile social coordination. It uses explicit lock/state transitions for deterministic parallel execution:

1. decompose work
2. assign isolated slices
3. lock shared resources
4. run in parallel
5. merge after verification

The design lesson is bigger than the implementation: parallelism only scales when conflict boundaries are machine-enforced.
</Card>

---

## <Label color="rose">9</Label> NTM — Named Tmux Manager

[namedtmuxmanager](https://github.com/Dicklesworthstone/namedtmuxmanager) (`ntm`) is the session substrate for multi-agent terminal workflows.

<Card>
### Operational role

NTM creates predictable named sessions for workers, reviewers, and monitors. In practice, this gives you reproducible terminal topology for long-running agent jobs: easy attach/detach, clearer ownership, and less session drift.

Think of NTM as process locality infrastructure for coordination-heavy runs.
</Card>

---

## <Label color="teal">10</Label> SLB — Simultaneous Launch Button

[simultaneous-launch-button](https://github.com/Dicklesworthstone/simultaneous-launch-button) (`slb`) launches multiple processes in parallel with coordinated output handling.

<Card>
### Where it fits

When a workflow needs many services or workers online at once, serial startup adds latency and operator friction. SLB compresses startup overhead and standardizes launch behavior, which is especially useful before benchmark runs and orchestration tests.
</Card>

---

## <Label color="purple">11</Label> ACFS — Agentic Coding Flywheel Setup

[agentic_coding_flywheel_setup](https://github.com/Dicklesworthstone/agentic_coding_flywheel_setup) (`acfs`) bootstraps the environment that the rest of the flywheel assumes.

<Card>
### Bootstrap philosophy

ACFS reduces setup entropy: install dependencies, provision runtime scaffolding, and make toolchain bring-up reproducible on fresh machines or VPS instances. This pushes setup from tribal knowledge into executable infrastructure.

In flywheel terms, ACFS is part of **Execute** because it ensures tasks can be run consistently before verification and learning begin.
</Card>

---

## <Label color="green">12</Label> CAAM — Coding Agent Account Manager

CAAM (Coding Agent Account Manager) handles OAuth/account token lifecycle across providers.

<Card>
### Why auth belongs in the flywheel

Multi-agent systems fail fast when credentials expire mid-run. CAAM externalizes token refresh, account switching, and auth-state hygiene so execution pipelines don't stall on credential drift.

It is deliberately boring infrastructure — and that is exactly why it is high leverage.
</Card>

---

## The Flywheel

These 12 tools aren't independent utilities — they form a closed loop:

<Pipeline
  nodes={[
    { title: "Plan", subtitle: "beads_rust + beads_viewer" },
    { title: "Coordinate", subtitle: "Agent Mail + Agent Farm + NTM + SLB" },
    { title: "Execute", subtitle: "ACFS + CAAM" },
    { title: "Verify", subtitle: "UBS + DCG" },
    { title: "Remember", subtitle: "CASS + CM" }
  ]}
  arrows={[
    { label: "prioritize", direction: "right" },
    { label: "orchestrate", direction: "right" },
    { label: "ship safely", direction: "right" },
    { label: "learn", direction: "right" }
  ]}
  footer="Plan → Coordinate → Execute → Verify → Remember → repeat. Each loop compounds: better planning inputs, safer execution defaults, and stronger procedural memory."
/>

1. **Plan:** `br ready` + `bv --robot-triage` selects highest-impact, dependency-safe work.
2. **Coordinate:** Agent Mail reservations and Agent Farm/NTM/SLB orchestration keep parallel workers from stepping on each other.
3. **Execute:** ACFS and CAAM keep environment and auth reliable while work is in-flight.
4. **Verify:** DCG blocks destructive commands, UBS scans for language-specific bug patterns.
5. **Remember:** CASS indexes session traces, CM promotes stable lessons into procedural rules.
6. **Repeat:** the next planning cycle starts with better priorities, better safeguards, and better memory.

## What makes this ecosystem different

- **Graph-first planning:** task priority is derived from dependency structure, not queue order.
- **Coordination as protocol:** messaging and file reservations are first-class, not ad hoc conventions.
- **Safety at command boundary:** destructive intent is intercepted before execution.
- **Verification tuned for agent failure modes:** UBS targets bug classes LLMs repeatedly generate.
- **Memory as compounding asset:** CASS + CM turn transient sessions into reusable operating rules.
- **Composable, single-purpose tools:** each utility does one job well and plugs into the loop.
- **Operational realism:** setup (ACFS), session topology (NTM/SLB), and auth hygiene (CAAM) are treated as core system concerns.

The full ecosystem is open source and actively evolving at [github.com/Dicklesworthstone](https://github.com/Dicklesworthstone), with 7,300+ combined stars across the public repos in this stack.
